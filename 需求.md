# 需求文档

## 项目目标 

1. 使用Python完成复杂逻辑的日销计算
2. 自动化、高效率
3. 生成可复用的代码，有新的文件需要计算日销时，我只需输入文件路径、选择文件或者更改文件路径的变量即可对新的文件进行操作

## 约束条件

1. 只准在最新的日销列进行操作，不得更改其他内容的格式
2. 保留嵌入单元格的图片

## 规则说明

1. 只有最新销量有值的日销需要计算
2. 日销计算逻辑：
   1. 销量<10000：(最新的销量 - 上一次有值的销量) / 两次数据相差的日期	
   2. 10000<=销量<100000：
      1. 最新销量和上一次销量有变动，则(最新销量 - 上一次销量的值) / 最新销量的日期和上一次销量的值第一次出现的日期的间隔
      2. 最新销量和上一次销量无变动：
         1. 若保持上一次的日销的情况下，`日销 * 日期 <= 1000`，则继续保持上一次的日销值
         2. 若保持上一次的日销的情况下，`日销 * 日期 > 1000`，则使用 `1000 / 日期` 作为新的日销值
   3. 销量 >= 100000：使用上一次有跳动的值进行计算，即若销量变动情况为：97000  99000  100000 则使用 `99000 - 97000 / 日期` 作为日销



