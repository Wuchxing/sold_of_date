# 项目规则

- 默认用中文沟通；需求不完整或存在影响结果的歧义时，先询问用户，不编造业务口径。
- 使用 Python 实现可复用的累计销量日销计算，支持输入路径及文件选择。
- 需求来源为 `需求.md` 及用户确认；精确行为以代码和测试为准，当前进度写入 `交接摘要.md`，不在聊天重复堆积实现记录。
- 仅计算最新销量有值的行，只写最新日销列。唯一例外：最新销量低于上一次有值销量时，将最新销量改为上一次销量，并将最新日销置零。历史列一律不改。
- 日销以引用相关单元格的 Excel 公式写入，并保留计算缓存；历史不足仍留空。销量下降时最新销量写回数值，最新日销写公式 `=0`。
- 最新日销整列复制上一列日销的格式，包含表头、空白单元格和整列默认样式、列宽；保留目标列可见性。历史列不改。
- 无历史销量留空；无历史日销根据累计销量回算。日销不为负数，允许小数，不使用 ROUND 四舍五入；公式保留 MAX(0, …) 非负保护，显示格式仍沿用上一列；另存新文件，禁止覆盖源文件。
- 销量中的“断货”和 `#N/A` 按缺失值跳过，向前寻找有效数字销量，保留实际记录日期计算间隔；不将缺失值当作零，不修改原始缺失标记。其他未知非数字值仍报错。
- 小于1万：本次与上一次有值销量之差除以日期间隔。
- 1万至10万以下：增长时，除以上一次销量连续出现区间首次记录到当前的天数；不变时，沿用上次日销，但不超过1000除以当前值连续出现区间首次记录到当前的天数。空记录跳过。
- 首次达到100000时，采用此前最近一次增长速度；10万以上出现新增长时更新速度，后续未增长则沿用。
- 保留全部图片、图片关系、非目标列样式及其他工作表。采用 XLSX ZIP 内目标单元格精确修改，禁止通过通用工作簿重导出导致图片或扩展信息丢失。
- 表头无年份时，以文件名日期的年份或 `--year` 为最新年份，从右向左处理跨年。
- 修改计算行为必须添加有业务意义的回归测试；文件集成测试必须验证非目标内容及图片不变。
